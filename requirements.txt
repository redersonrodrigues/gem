# Requisitos para aplicação desktop GEM (PySide6, SQLAlchemy, etc.)

# GUI
PySide6              # Interface gráfica (Qt6 para Python)
qtawesome            # Ícones FontAwesome offline para Qt

# ORM e Banco de Dados
SQLAlchemy           # ORM para SQLite e outros bancos
alembic              # Migrations/versionamento do banco

# Segurança
passlib              # Hash de senhas
bcrypt               # Algoritmo de hash seguro (usado pelo passlib)
cryptography         # Criptografia de dados sensíveis

# Testes
pytest               # Testes unitários/integrados
pytest-qt            # Testes para interface Qt
pytest-cov           # Cobertura de testes
pytest-benchmark     # Testes de performance

# Logging
loguru               # Logging avançado (opcional, pode usar stdlib)

# Visualização e relatórios
matplotlib           # Gráficos/dashboards
seaborn              # Gráficos avançados
pyqtgraph            # Gráficos interativos para Qt
pandas               # Manipulação e exportação de dados (CSV, etc.)

# Utilitários e produtividade
black                # Autoformatação de código (opcional)
isort                # Organização de imports (opcional)

# Outras dependências úteis, se necessário:
# APScheduler         # Tarefas agendadas (se usar)
# fastapi             # API REST (opcional, integração futura)
# pydantic            # Validação de dados (opcional)
# reportlab           # Geração de PDF (opcional)
# python-dotenv       # Variáveis de ambiente (opcional)

# Linha abaixo para manter compatibilidade com versões
# Adicione versões conforme estabilidade do projeto, por exemplo:
# PySide6==6.7.0

